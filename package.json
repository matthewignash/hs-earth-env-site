{
  "name": "hs-earth-env-site",
  "version": "0.1.0",
  "private": true,
  "description": "HTML prototype of the HS Earth & Environmental Science course Site (AISC, AY 2026-27).",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "sync-simulator": "cp ../Plate-Tectonics-City-Builder/tectonic-city-builder.html src/assets/simulators/tectonic-city-builder.html && echo 'Simulator synced from sibling folder.'"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
